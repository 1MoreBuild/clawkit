[exclude-files]
# lockfile integrity hashes create high-entropy noise
pattern = (^|/)pnpm-lock\.yaml$
# baseline/config files should not be scanned as source
pattern = (^|/)\.secrets\.baseline$
pattern = (^|/)\.detect-secrets\.cfg$
